# 数据管线（子项目 B）运行依赖。
# 真实采集（baostock）与可选 akshare 在无外网环境不可用；--mock 离线模式与 pytest 不依赖它们。

# 真实采集（A 股日 K + 停牌/ST/复权因子）。真实抓取时需要 + 外网。
baostock>=0.8.8

# 可选：补充数据源（退市名单、流通股本等）。按需启用。
# akshare>=1.12.0

# 数据处理（管线核心实现仅用标准库，pandas/numpy 仅在扩展真实清洗时可选）。
# pandas>=1.5
# numpy>=1.23

# 测试
pytest>=7.0
